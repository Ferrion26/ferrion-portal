# deps
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# build
/.next/
/out/
/dist/

# env
.env
.env.local
.env.*.local

# collector — real config holds a customer API key + device credentials
collector/config.json
collector/logs/

# vendor REST API reference docs (confidential Huawei partner docs — keep local only)
docs/Rest/
docs/Quality/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
/coverage

# typescript
*.tsbuildinfo
next-env.d.ts

# raw partner-portal logo downloads (zips/source files) — only the
# processed asset actually used in code (e.g. public/logos/fudo-mark.png)
# is committed
/public/logos/Fudo/
/public/logos/CommVault/

# raw/unused source files kept locally for reference, not shipped
/obsolete/
